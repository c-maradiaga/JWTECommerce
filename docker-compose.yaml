services:
  sqlserver-permissions:
    image: busybox
    command: [ "sh", "-c", "chown -R 10001:0 /var/opt/mssql/data && chmod -R 770 /var/opt/mssql/data" ]
    volumes:
      - sqlserver-data:/var/opt/mssql/data
    user: "0:0"
    restart: "no"
    networks:
      - app-network

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver-db
    platform: linux/amd64
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=${SA_PASSWORD}
    ports:
      - "1434:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql/data
    depends_on:
      sqlserver-permissions:
        condition: service_completed_successfully
    healthcheck:
      test: [ "CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P '$SA_PASSWORD' -C -Q 'SELECT 1' || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - app-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web-api
    ports:
      - "8081:8055" # Puerto_Externo:Puerto_Interno (8081 instancia del servidor/host (donde corre Coolify) 8055 instancia del contenedor (donde corre Kestrel))
    environment:
      # Sobrescribimos la URL para forzar el puerto 8055 (Sin espacios alrededor de '=')
      - ASPNETCORE_URLS=http://+:8055
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=JWTECommerce;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True;Encrypt=True;
    depends_on:
      sqlserver:
        condition: service_healthy
    networks:
      - app-network
      - coolify

volumes:
  sqlserver-data:

networks:
  app-network:
    driver: bridge
  coolify:
    external: true
    ##* PUNTOS CLAVE:
    #? 1. La API se expone públicamente a través de Traefik, mientras que SQL Server solo es accesible internamente.
    #? 2. La contraseña de SQL Server se gestiona mediante la variable de entorno SA_PASSWORD    
    #? 3. loadbalancer.server.port=8080 → este es el puerto interno en que Kestrel escucha dentro del contenedor 
    #    (por defecto en .NET 8+ es 8080 para HTTP cuando corre en contenedor Linux — lo confirmamos en el Paso 3).
    #? 4. La red internal (privada, solo entre tu API y tu SQL Server) es distinta de coolify (donde vive Traefik) — 
    #    así el SQL Server nunca es alcanzable desde fuera, ni siquiera por Traefik
    #? 5. El networks: external: true en coolify le dice a Docker Compose "esta red ya existe, no la crees, solo únete a ella".

    # services:
    #   sqlserver2022:
    #     image: mcr.microsoft.com/mssql/server:2022-latest
    #     container_name: sqlserver2022
    #     environment:
    #       ACCEPT_EULA: 'Y'
    #       MSSQL_SA_PASSWORD: 'Pass123**'
    #       MSSQL_PID: 'Developer'
    #     ports:
    #       - "1434:1433"

    #     volumes:
    #       - jwt_ecommerce:/var/opt/mssql

    # volumes:
    #   jwt_ecommerce:

    #? Recordar tener Docker instalado y en ejecución para usar este archivo docker-compose.yaml.
    #? en el apartdo ports: 1434:1433, el puerto 1434 es el puerto del host (nuestro equipo) y el puerto 1433 es el puerto del contenedor (sqlserver2022). Esto significa que cuando accedamos a localhost:1434 en nuestro equipo, estaremos accediendo al puerto 1433 dentro del contenedor, donde se está ejecutando SQL Server.

    #* Comandos útiles:
    # Levantar los contenedores en segundo plano:
    #*   docker-compose up -d
    # Apagar y eliminar contenedores (no borra los volúmenes):
    #*   docker-compose down
    # detener un contenedor específico:
    #*   docker stop sqlserver2022
    # eliminar un contenedor específico:
    #*   docker rm sqlserver2022



